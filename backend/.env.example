# Environment variables for Training Data Generator Backend
# Copy this file to .env and fill in your API keys

# OpenAI API Key for sentence generation (required for OpenAI provider)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# LLM Provider: "openai" or "ollama" (default: openai)
LLM_PROVIDER=openai

# Ollama settings (only used when LLM_PROVIDER=ollama)
# Make sure Ollama is running: ollama serve
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Google Cloud credentials file path (required for TTS)
# Place your service account JSON file in the backend folder
GOOGLE_APPLICATION_CREDENTIALS=google_credentials.json

# Optional: Frontend URL for CORS (comma-separated for multiple)
# CORS_ORIGINS=http://localhost:5173,https://your-production-domain.com
